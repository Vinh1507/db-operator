---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: clusterbackups.dbaas.example.com
spec:
  group: dbaas.example.com
  names:
    kind: ClusterBackup
    listKind: ClusterBackupList
    plural: clusterbackups
    shortNames:
    - cbk
    singular: clusterbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - jsonPath: .spec.engineName
      name: Engine
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.destination
      name: Destination
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterBackup is the Schema for the clusterbackups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterBackupSpec defines the desired state of ClusterBackup
            properties:
              clusterName:
                description: ClusterName is the name of the Cluster to backup
                type: string
              engineName:
                description: EngineName is the name of the engine instance to backup
                type: string
            required:
            - clusterName
            - engineName
            type: object
          status:
            description: ClusterBackupStatus defines the observed state of ClusterBackup
            properties:
              completedAt:
                description: CompletedAt is when the backup completed
                format: date-time
                type: string
              createdAt:
                description: CreatedAt is when the backup was created
                format: date-time
                type: string
              destination:
                description: Destination is the backup location
                type: string
              inUse:
                default: false
                description: InUse indicates if backup is being used for restore
                type: boolean
              message:
                description: Message provides additional information
                type: string
              state:
                description: State is the current state of the backup
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
